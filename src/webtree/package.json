{
  "name": "alakazam-workspacetree",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for src/WorkSpaceTree.html: a self-contained uihtml page wrapping yy-tree, used by WorkSpaceTree.m.",
  "scripts": {
    "build": "node build.mjs && node assemble.mjs",
    "test": "node test_node.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "yy-tree": "^0.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsdom": "^24.1.3"
  }
}
